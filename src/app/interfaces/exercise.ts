export interface Exercise {
    id?: number,
    // se pueden poner las key con " y sin "? se pueden poner todas las keys sin comillas
    "name": string,
    "sets": number,
    "repetitions": number,
    "weight": number
}
